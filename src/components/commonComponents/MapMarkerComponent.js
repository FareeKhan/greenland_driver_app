import React from 'react'
// import { Marker } from 'react-native-maps';

const MapMarkerComponent = (props) => {
   // const { coordinate, children, title, description } = props;

   return (
      <>
         {/* <Marker
            coordinate={coordinate} title={title} description={description} >
            {children}
         </Marker> */}
      </>
   )
}

export default MapMarkerComponent